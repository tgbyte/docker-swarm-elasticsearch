#/bin/bash
SWARM_SERVICE_IPs=$(dig tasks.${SERVICE_NAME} +short)
if [ -n "${SWARM_SERVICE_IPs}" ]; then
  (curl -s --fail http://$(hostname):9200/_cluster/health?local=true | grep -q "green") || exit 1
fi
exit 0
